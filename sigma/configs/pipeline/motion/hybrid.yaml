name: hybrid
implementation: sigma.pipeline.motion.hybrid.HybridMotionEstimator
matcher: lightglue
flow_model: raft
confidence_threshold: 0.85
device: ${device}
visualization: true
min_mask_ratio: 0.01
min_mask_pixels: 500
min_component_pixels: 1000
top_percent: 0.8
